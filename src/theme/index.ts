import { createTheme } from '@mui/material';

export const themeOptions = createTheme({
  palette: {
    primary: {
      main: '#297EFF',
    },
    secondary: {
      main: '#f50057',
    },
  },
});
