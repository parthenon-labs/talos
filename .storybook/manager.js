import { addons } from '@storybook/addons';
import { create } from '@storybook/theming';

addons.setConfig({
  theme: create({
    brandImage: 'https://yidengfe.com/images/logo2.ecf35.bundule.png',
  }),
});
